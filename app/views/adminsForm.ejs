<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    /* Add custom styles for the navigation bar and search bar */
    body {
      color: #000000;
      margin: 0;
      padding: 0;
    }
    .navbar{
      background-color: #ffffff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }
    .navbar a {
      text-decoration: none;
    }
    .navbar .navbar-right {
      float: right;
    }
    /* Add custom styles for the main content */
    .main-content {
      padding: 5% 0;
      text-align: center;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh;
    }
    .main-content h1 {
      margin-top: 0;
      font-size: 3rem;
      color: #000000;
    }

    /* Add custom styles for the login form */
    .login-form {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background-color: #f5f5f5;
    }
    .login-form input[type="text"],
    .login-form input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
    .login-form button {
      width: 100%;
      padding: 10px;
      color: #fff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    .table{
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }
  </style>

<script>
  function confirmDelete() {
    return confirm("คุณต้องการลบสินค้านี้ใช่ไหม?");
  }
</script>
</head>
<body class="bg-light">
  <!-- Navigation bar -->
  <nav class="navbar navbar-expand-lg navbar-light ">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">KGN<i class="bi bi-2-circle-fill"></i>HAND</a></a>
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <a class="nav-link" href="/"><i class="bi bi-house-fill"></i> Home</a>
          <a class="nav-link" href="/insertadmin"><i class="bi bi-person-fill-add"></i> เพิ่มสิทธิ์ผู้ดูแลระบบ (Add Admin)</a>
        </li>
      </ul>
    <div class="navbar-right">
      <div class="left-aligned-text">
        <span>For Admin Only</span>
        <a href="/" class="btn btn-outline-success"><i class="bi bi-box-arrow-left"></i> ออกจากระบบ</a>
      </div>
    </div>
  </div>
</nav>

<!-- Main content -->
<div class="main-content">
  <div class="container text-center">
    <h1 class="mt-4">Products All Lists</h1>
    <form class="d-flex justify-content-center mb-4" method="get" action="/searching">
      <div class="input-group mb-3">
        <input type="text" name="q" class="form-control" placeholder="ค้นหา (ชื่อ)" value="">
        <button type="submit" class="btn btn-primary">ค้นหา</button>
        <a href="/insertForm" class="btn btn-success ">เพิ่มรายการสินค้า<i class="bi bi-plus"></i></a>
      </div>
    
    </form>

    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Product ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Stock</th>
            <th>Image</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(function(item, index) { %>
          <tr>
            <td><%= item.ProductID %></td>
            <td><%= item.ProductName %></td>
            <td><%= item.ProductPrice %></td>
            <td><%= item.UnitsInStock %></td>
            <td><%= item.Image %></td>
            <td><a href="/edit/<%=item.ProductID%>" class="btn btn-warning btn-sm"><i class="bi bi-pencil-square"></i> Edit</a>
            <a href="/delete/<%= item.ProductID %>" onclick="return confirmDelete()" class="btn btn-danger btn-sm"><i class="bi bi-trash3-fill"></i> Delete  </a></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="text-center">
    <a href="/" class="btn btn-success">หน้าแรก</a>
  </div>
</div>
</body>
</html>

