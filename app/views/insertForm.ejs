<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>เพิ่มรายการสินค้าสต๊อกของร้านค้า Shop store</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    /* Add custom styles for the main content */
    .main-content {
      padding: 10% 0;
      text-align: center;
      background-color: #f8f9fa; /* Light gray background */
      min-height: 100vh;
    }

    /* Style for the jumbotron */
    .jumbotron {
      background-color: #b59f7a; /* Earthy brown color */
      color: #fff;
      padding: 20px;
      margin-bottom: 0;
    }

    /* Style for the form */
    .form-container {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #e3e3e3; /* Light gray border */
      border-radius: 5px;
      background-color: #fff;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Light shadow */
    }

    /* Style for the form labels */
    .form-label {
      font-weight: bold;
      color: #333; /* Dark gray color for labels */
    }

    /* Style for the form inputs */
    .form-control {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc; /* Light gray border */
      border-radius: 3px;
    }

    /* Style for the submit button */
    .btn-success {
      width: 100%;
      padding: 10px;
      color: #fff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      transition: background-color 0.3s ease-in-out;
    }
  </style>
</head>

<body>
  <div class="jumbotron text-center bg-dark">
    <h2>เพิ่มรายการสินค้าสต๊อกของร้านค้า Shop store</h2>
  </div>
  <div class="main-content">
    <div class="form-container">
      <h5 class="mb-4">เพิ่มรายการสินค้า</h5>
      <form action="/insert" method="POST" onsubmit="return confirmSubmit()" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="ProductName" class="form-label">ชื่อสินค้า</label>
          <input type="text" class="form-control" id="ProductName" name="ProductName" required>
        </div>
        <div class="mb-3">
          <label for="ProductPrice" class="form-label">ราคา</label>
          <input type="number" class="form-control" id="ProductPrice" name="ProductPrice" required>
        </div>
        <div class="mb-3">
          <label for="UnitsInStock" class="form-label">จำนวนสินค้าในสต็อก</label>
          <input type="number" class="form-control" id="UnitsInStock" name="UnitsInStock" required>
        </div>
        <div class="mb-3">
          <label for="ProductImage" class="form-label">รูปภาพสินค้า</label>
          <input type="file" class="form-control" id="Image" name="Image" accept="image/*" required>
        </div>
        <button type="submit" class="btn btn-success">บันทึก</button>
      </form>
    </div>
  </div>

  <!-- JavaScript code to show the confirmation dialog -->
  <script>
    function confirmSubmit() {
      return confirm("คุณแน่ใจหรือไม่ที่ต้องการบันทึกสินค้า?");
    }
  </script>
</body>

</html>
