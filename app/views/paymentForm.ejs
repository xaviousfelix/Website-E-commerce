<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>
    /* Add custom styles for the navigation bar and search bar */
  
    .navbar {
      background-color: #17a5ba67;
      color: #070707;
      padding: 30px;
    }
    .navbar a {
      color: #060606;
    }
    .navbar .navbar-right {
      float: right;
      padding-right: 20px;
    }
    .search-container {
      float: right;
      margin-right: 10px;
    }
    .search-container input[type="text"] {
      padding: 6px;
      border: none;
      width: 200px;
    }
    .search-container button {
      padding: 6px 10px;
      background: #333;
      color: #fff;
      border: none;
      cursor: pointer;
    }
    /* Add custom styles for the main content */
    .main-content {
      padding: 20px;
      text-align: center;
    }
  </style>
  <style>
    .left-aligned-text {
      text-align: left;
    }
  </style>
</head>
<body >
  <!-- Navigation bar -->
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
       <h4> <a class="navbar-brand" href="#">Restaurant</a></h4>
      </div>
      <h4><ul class="nav navbar-nav">
        <li><a href="#">Home</a></li>
        <li><a href="/product">Products</a></li>
        <li><a href="#">Service</a></li>
        <li><a href="#">Press</a></li>
        <li><a href="#">Contact Us</a></li>
      </ul></h4>
    </nav>
    </body>
<!-- Payment form -->
<div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3 payment-form">
        <h2>ชำระเงิน</h2>
        <form  action="/payment/<%= customerID %>/<%= totalAll %>" method="POST" >
          <div class="form-group">
            <label for="name">ชื่อ-สกุล ผู้รับ</label>
            <input type="text" class="form-control" id="name" name="PayName" required>
          </div>
          <div class="form-group">
            <label for="address">ที่อยู่ในการจัดส่ง</label>
            <input type="text" class="form-control" id="address" name="PayAddress" required>
          </div>
          <div class="form-group">
            <label for="phone">เบอร์โทรศัพท์</label>
            <input type="tel" class="form-control" id="phone" name="PayTel" required>
          </div>
          <div class="form-group">
            <label for="amount">จำนวนเงิน</label>
            <input  class="form-control" id="amount" value="<%= totalAll %>" required readonly disabled>
          </div>

          <div class="form-group">
            <label for="slip">สลิปโอนเงิน</label>
            <input type="file" class="form-control" id="slip" name="SlipPayment" accept="image/*" required>
          </div>
          
          <input type="hidden" name="cmID" id="hiddencmID" value='<%= customerID %>'>
          <input type="hidden" name="TotalAll" id="hiddenTotalAll" value='<%= totalAll %>'>
          <button type="submit" class="btn btn-primary">ยืนยันข้อมูลชำระเงิน</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>