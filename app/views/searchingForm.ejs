<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* Add custom styles for the navigation bar and search bar */
        body {
            background-color: #d6d6d6;
            color: #fff;
            margin: 0;
            padding: 0;
        }

        .navbar a {
            color: #fff;
        }

        .navbar .navbar-right {
            float: right;
        }

        .search-container {
            float: right;
            margin-right: 10px;
        }

        .search-container input[type="text"] {
            padding: 6px;
            border: none;
            width: 200px;
            background-color: #333;
            color: #fff;
        }

        .search-container button {
            padding: 6px 10px;
            background: #333;
            color: #fff;
            border: none;
            cursor: pointer;
        }

        /* Add custom styles for the main content */
        .main-content {
            padding: 5% 0;
            text-align: center;
            background-color: #ccc;
            background-position: center;
            background-repeat: no-repeat;
            min-height: 100vh;
        }

        .main-content h1 {
            margin-top: 0;
            font-size: 3rem;
            color: #ffffff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        /* Add custom styles for the login form */
        .login-form {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #f5f5f5;
        }

        .login-form input[type="text"],
        .login-form input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }

        .login-form button {
            width: 100%;
            padding: 10px;
            background-color: #17a5ba67;
            color: #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }
    </style>
    </style>
    <script>
        function confirmDelete() {
            return confirm("คุณแน่ใจหรือไม่ที่ต้องการลบสินค้านี้?");
        }
    </script>
</head>

<body>
    <!-- Navigation bar -->
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">KGN2HAND</a>
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/customer_auth/productsForm">Products</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contact Us</a>
                </li>
            </ul>
            <div class="navbar-right">
                <div class="left-aligned-text">
                    <span>สิทธิ์การใช้งานเฉพาะผู้ดูแลระบบเท่านั้น</span>
                    <a href="/" class="btn btn-success">ออกจากระบบ</a>
                </div>
            </div>
        </div>
    </nav>
    <!-- Login and cart -->
    <div class="navbar-right">
        <!-- Authorize admin -->
        <div class="left-aligned-text">
            <span>Authorize admin</span> <a href="/" class="btn btn-success">Logout</a>
        </div>
    </div>
    </div>
    </nav>

    <!-- Product List -->
    <div class="container mt-4">
        <h1 class="text-center">แสดงรายการสินค้าที่ค้นหา</h1>
        <form class="form-inline justify-content-center mb-4" method="get" action="/searching">
            <div class="form-group mx-sm-3">
                <div class="input-group mb-3">
                    <input type="text" name="q" class="form-control" placeholder="ค้นหา (ชื่อ)" value=""><br>
                    <button type="submit" class="btn btn-primary">ค้นหา</button>
                    <a href="/inserForm" class="btn btn-success mx-2">เพิ่มรายการสินค้า</a>
                    <a href="/admins_auth/adminsForm" class="btn btn-primary">แสดงสินค้าทั้งหมด</a>
                </div>
            </div>

        </form>

        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Product ID</th>
                        <th>Name</th>
                        <th>Price</th>
                        <th>Stock</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                </thead>
                <tbody>
                    <% items.forEach(function(item, index) { %>
                        <tr>
                            <td>
                                <%= item.ProductID %>
                            </td>
                            <td>
                                <%= item.ProductName %>
                            </td>
                            <td>
                                <%= item.ProductPrice %>
                            </td>
                            <td>
                                <%= item.UnitsInStock %>
                            </td>
                            <td><a href="/edit/<%= item.ProductID %>">แก้ไข</a></td>
                            <td><a href="/delete/<%= item.ProductID %>" onclick="return confirmDelete()">ลบ</a></td>
                        </tr>
                        <% }); %>
                </tbody>
            </table>
        </div>
        <div class="text-center">
            <a href="/" class="btn btn-success">หน้าแรก</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>